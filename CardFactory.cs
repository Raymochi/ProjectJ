public class CardFactory {
	
	private Dictionary<int, Card> cards;

	public CardFactory() {
		cards = new Dictionary<int, Card>();
	}

	public void LoadCards(string cardFile) {
		
		// Read file and loop
			// cardId = ... card id ...
			// card = new card(cardId);
			// cards[card.Id] = card
	}
}